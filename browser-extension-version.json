{
  "latest": "0.2.7",
  "minimum": "0.2.0",
  "notes": "Fixes Capture-all on Better Content directories whose guide cards use rendered click navigation instead of hrefs, prevents directory shells from being queued as the only page, and adds minimize/stop/hide controls plus terminal auto-dismiss to the in-page progress HUD."
}
