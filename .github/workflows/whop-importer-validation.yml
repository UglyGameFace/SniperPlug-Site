name: SniperPlug maintenance pause

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The permanent validation workflow is restored after the hard-free file set lands."
